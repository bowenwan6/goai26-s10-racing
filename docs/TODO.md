# Todo

## 记录约定

- 调用时机：出现新的明确待办、优先级变化或任务完成时更新；用户可随时手动维护，LLM 只按需辅助整理。
- 记录内容：使用复选框，按 P0、P1、P2 排序；每项写清完成条件，不把没有证据的设想加入待办。
- 优先级：P0 阻塞使用，P1 近期应做，P2 可延后。

## 已完成

- [x] 48号重启后验证IMU时间与ODOM恢复，核实103→106授时链路及106发布源，部署PTP启动检查和手机测量时间拦截；实机只读及离线检查通过。（2026-09-11）
- [x] 登录 48 号 106 确认官方 SLAM 3.5.1 和 drmap；参照 xwy 热点副本部署 103 入口、102 网页和按需数据读取，通过真实雷达预览与接口检查。（2026-09-11）

- [x] 将当前真机入口更正为 48 号 golai，区分 50 号 SLAM 历史证据；补充可分发的只读 SLAM 检查脚本和独立 GUI 消息环境，完成 ARM 构建与 GUI 实机只读状态验证。（2026-09-11）
- [x] 完成 50 号独立采集部署清理：102 采集站/热点、自启、本机口令和 103 临时中转目录均已删除；原服务和本地录制保留。（2026-09-10）

- [x] 审查 9 轮历史录制并提取 50 Hz 关节参考和高台 10 Hz SLAM 路径；明确缺少教师动作、低频片段与未标注结果。（2026-09-09）
- [x] 完成 SDK 关节映射、运动学回放、短时平地跟踪和 72 组假设楼梯试配；两段下楼通过所选场景终点判据，上楼未通过。（2026-09-09）
- [x] 下载并研究 xwy 的地图、只读验证和楼梯模型目录，导出完整点云和离线查看器。（2026-09-09）
- [x] 只读登录 106，确认厂商 `slam 3.5.1` 的 LIO/回环/图优化结构，整理算法来源证据与不确定项。（2026-09-09）
- [x] 将研究结论、地图预览、材料边界和复现条件整理到 Git 文档，更新采集站当前入口。（2026-09-09）

- [x] 用户已为 `goai2026` 设置私密密码；`passwd -S goai2026` 状态为 `P`。（2026-08-14）
- [x] 修复进入 `rl_control` 后终端 WASD 仍被清零的问题；隔离仿真中 W/S 轮速命令最大均值差为 31.731。（2026-08-14）
- [x] 将 viewer 同步降至约 60 Hz，并在手动模式跳过 lidar/高度图 raycast；隔离验证中 odom 保持发布且 `/scan` 为 0 条。（2026-08-14）
- [x] 修复非交互启动时 WSLg 回退到 `llvmpipe` 的问题；清空外部变量后启动脚本仍确认 RTX 4060 D3D12 renderer。（2026-08-14）
- [x] 将手动 viewer 拆为独立 ROS 进程；显示开启时关节发布由旧架构约 165 Hz 提升至约 187 Hz，窗口不再阻塞控制。（2026-08-14）
- [x] 用 GLFW Wayland variant 对照 WSLg viewer；用户确认窗口仍卡，因此不再继续调 WSLg 参数。（2026-08-14）
- [x] 将手动显示迁移到 Windows 原生 MuJoCo viewer；Win32 WGL 使用 RTX 4060，真实 ROS UDP 状态流已连接。（2026-08-14）
- [x] 人工验收 Windows 原生 MuJoCo viewer 流畅度；用户实际运行后确认画面已经很流畅。（2026-08-14）

## P1

- [ ] 时间修复后重做48号短距离闭环：原手机走场已保存，但跳时、零回环且首尾差约103米；完成条件是新会话输入持续正常、回到起点的轨迹与地图一致。
- [ ] 下次正常开机验收106新增PTP启动检查，并确认最初3月→9月跳时由谁触发；当前系统日期、IMU和ODOM已恢复，不能将一次重启恢复等同于长期授时策略已验收。
- [ ] 区分48号两个原始点云写者的实际发包，核对坐标与时间；LIO_ODOM有发布者无消息，未启动mapping时不要求SLAM_ODOM存在。

- [ ] 在 Windows 原生 MuJoCo viewer 中人工驾驶一次完整键位流程；完成条件：确认 Z/C、W/A/S/D、Q/E 和松键停止符合预期。
- [ ] 审阅实际暂存列表后由用户 commit/push；文档整理没有暂存、提交或推送，其他代码改动仍需按本次提交范围检查。
- [ ] 共享历史研究包给队友并补上获取入口；完成条件：队友能拿到文档列出的数据与脚本、离线打开地图，认证文件不在共享包内。
- [ ] 标定或核对 IMU/SLAM 与机身外参及跨设备时间；完成条件：记录转换与误差，确认高台位姿的参考点，并解释在线/保存轨迹差异。
- [ ] 为楼梯片段补充点云/实测尺寸、相对位置和成功/失败标注；完成条件：场景与接触解释得到验证，上楼失稳片段不进入成功专家集。
- [ ] 接入现有 Isaac Lab 环境做短片段参考跟踪；完成条件：先核对机器人资产与动作映射，再报告跟踪误差、接触和越障结果，合成路径与实测标签分开。

## P2

- [ ] 若需复现厂商 SLAM 内部算法，向厂商核对 `slam 3.5.1` 前端来源、实际实现版本与轨迹保存语义；现有头文件不足以确定完整派生版本。
