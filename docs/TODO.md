# Todo

## 记录约定

- 调用时机：出现新的明确待办、优先级变化或任务完成时更新；用户可随时手动维护，LLM 只按需辅助整理。
- 记录内容：使用复选框，按 P0、P1、P2 排序；每项写清完成条件，不把没有证据的设想加入待办。
- 优先级：P0 阻塞使用，P1 近期应做，P2 可延后。

## 已完成

- [x] 用户已为 `goai2026` 设置私密密码；`passwd -S goai2026` 状态为 `P`。（2026-08-14）
- [x] 修复进入 `rl_control` 后终端 WASD 仍被清零的问题；隔离仿真中 W/S 轮速命令最大均值差为 31.731。（2026-08-14）
- [x] 将 viewer 同步降至约 60 Hz，并在手动模式跳过 lidar/高度图 raycast；隔离验证中 odom 保持发布且 `/scan` 为 0 条。（2026-08-14）
- [x] 修复非交互启动时 WSLg 回退到 `llvmpipe` 的问题；清空外部变量后启动脚本仍确认 RTX 4060 D3D12 renderer。（2026-08-14）
- [x] 将手动 viewer 拆为独立 ROS 进程；显示开启时关节发布由旧架构约 165 Hz 提升至约 187 Hz，窗口不再阻塞控制。（2026-08-14）
- [x] 用 GLFW Wayland variant 对照 WSLg viewer；用户确认窗口仍卡，因此不再继续调 WSLg 参数。（2026-08-14）
- [x] 将手动显示迁移到 Windows 原生 MuJoCo viewer；Win32 WGL 使用 RTX 4060，真实 ROS UDP 状态流已连接。（2026-08-14）
- [x] 人工验收 Windows 原生 MuJoCo viewer 流畅度；用户实际运行后确认画面已经很流畅。（2026-08-14）

## P1

- [ ] 在 Windows 原生 MuJoCo viewer 中人工驾驶一次完整键位流程；完成条件：确认 Z/C、W/A/S/D、Q/E 和松键停止符合预期。
- [ ] 审阅当前暂存改动后决定是否 commit/push；当前分支为 `Jackdev`，本次未擅自提交或推送。

## P2

- 暂无已确认事项；由用户按需添加。
