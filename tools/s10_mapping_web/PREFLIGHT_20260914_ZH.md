# 2026-09-14 现场助手出发前检查

## 结论

本次获准的修复已部署：**掉定位不能复用旧检查，默认地图为 `0914_fr_v3-20260914-142008`**。网页和现场助手已重启，定位、规划服务及 active 地图未改变。没有发送运动指令、切图、启动真机录制或回放。

**目前暂停切图和外出验收。** 已有真机自检复查通过，但切图的“导航空闲/零运动反馈”条件未满足；用户补充队友几分钟前在测试自己的 policy，随后明确表示“还没确认”策略是否完全停止。必须先确认该策略完全停止发指令，再复核停稳条件。未放宽或绕过任何停稳门禁。

## 修复内容

- 静止检查增加持久的 `eligible_check`，健康异常、切图、后台重启和新的检查均撤销旧资格。
- 新检查失败也不能回退到旧的成功检查；后台恢复正常后必须重新采样30秒并人工核对。
- 人工确认以数据库事务原子检查资格，避免检查期间失效后又恢复旧授权。
- 前端只提供后台仍有效的检查供确认，不从历史成功记录自行推断。
- 默认目标改为 v3；v1 等其他目标的历史会话不再自动恢复覆盖默认值，历史仍可手动查看。

本次采用 API Testing 的回归与真机接口核查、Safety System 的失效撤销/人工接管边界、代码检视及手机宽度浏览器测试；未据此声称功能安全认证或无 bug 保证。

## 本地验证

- 独立 QA：72 项 Python 测试通过（包含9项新增失效证据回归）。
- 主测试：9项 Python 测试通过。
- 旧网页回归：`test_server.py` 和 `test_localization.cjs` 通过。
- 浏览器使用本地隔离 fake 后台，390×844：默认 v3、拒绝自动恢复 v1、新会话、假切图、30秒检查、预览、人工确认、10秒演示录制、刷新恢复、3秒草稿标点、汇总均完成。
- 页面宽度390、内容宽度390，无横向溢出。假录制不产生合格真机 bag，草稿仍为 `navigation_valid:false`。
- 语法与差异检查通过；未在机器人上注入断电、掉定位或磁盘满故障。

## 真机验证与保留异常

通过现有 Tailscale 用户态 SSH 通道访问102网页及106 worker，未更改 Clash、系统路由、热点转发或认证。

| 检查 | 结果 |
| --- | --- |
| `/`、`/field`、`/field.js`、`/localization`、`/heightmap` | HTTP 200 |
| worker | 在线，`demo:false` |
| 存储 | 12.10 GiB 可用；最低保留2 GiB |
| 必需 ROS 话题与 MCAP 依赖 | 存在 |
| v3 预览 | 29,876 个显示抽样点，地图身份匹配 |
| 自检结果文件下载 | HTTP 200，SHA256 校验匹配 |
| 当前 active 图 | `indoor_loop_01-20260911-203447`，没有切换为 v3 |
| 参考点/外参 | 未验证；只能保存位置草稿 |

第一次自检任务虽然 `SUCCEEDED`，但质量 `passed:false`：点云源时间落后约4.54秒。之后连续10次采样的点云源延迟0.17–0.42秒，第二次自检 `passed:true`。**保留两份报告，不用重试成功掩盖首次异常；这不能证明长时间链路稳定，也尚不能判定驱动故障。**

本地证据目录：`map-reviews/0914_fr_v3-20260914-142008/app-preflight-20260914/`（位于 GOAI 工作区，不在本 Git 仓库）：

- `http-preflight.json`：首次异常。
- `http-preflight-recheck.json`：复查通过。

17:23:57附近读取的最近73条厂商监视记录，转向命令均为 `0.000`，运动反馈均为 `-0.001`；同时显示 `Goal[NO]`、Code999。现有解析器要求反馈绝对值小于0.0005，故 `navigation.idle:false`，加载新图会被拦住。这只是对现有日志和代码的判断，没有真的执行切图测试。

用户已确认手柄停稳，并补充队友此前测试 policy。**当前证据不足以区分反馈残留/零偏、微动或另一控制程序的影响，也不能以 planner 的零命令证明所有外部策略停止。** 先协调停止所有其他运动命令源；本次未修改这一阈值。

## 部署与恢复

仅更新以下文件，源文件 SHA256：

| 文件 | SHA256 | 节点 |
| --- | --- | --- |
| field_core.py | c46d38aaa66e93a75a397d94bc1aae0ef2f136669bfa71f07b0b30450836f639 | 102、106 |
| field_worker.py | cd43a770b48b3585a92e3d9f63cbaf3e4bc8522f6d7e6b5a5b3649fa4668a229 | 106 |
| field.js | 66d58d5893d703b9c47a0c32d653c7f488eb5a40077d1f0d6f5469075346be80 | 102 |
| field.html | d68722bda23c983a3c21936da3e254e51c5bac1562115e84f1c997f6370d3f19 | 102 |

备份：

- 102：`/home/golai/s10-field-fix-20260914.UCSZTd/before/`
- 106：`/home/user/s10-field-fix-20260914.W72OvV/before/`

106更新前取得应用操作锁，确认无活动作业；只重启 `s10-field-worker.service`。102只重启用户 `s10-mapping-web.service`。定位 PID2432 / Invocation `d2a32305f5564f359ee3686c834da320`，规划 PID2945 / Invocation `dad34bd7a1994ee086a33e85760db5ac` 均未改变。

如需回滚，维护者应先确认无人操作且无活动作业，再用上述各节点备份恢复对应文件，只重启相应应用服务。保留 SQLite、报告和地图，不自动恢复旧的人工确认，不自动切图。旧版含已知证据复用漏洞，回滚后不能继续把旧检查用于现场验收。

## 后续现场顺序（前置阻碍解除后）

1. 请队友退出 policy，确认没有其他运动命令源；原厂手柄接管可用。
2. 出门前手机关蜂窝、连048热点，打开 `http://10.21.41.1:8080/` → 绿色“现场助手”。检查没有演示水印、默认 v3、自检必需项通过。手机无外网登录、刷新和下载需现场实测；目前未完成这一关。
3. 到 v3 覆盖内的明确起点附近安全平地停稳，加载 v3 原始地图；等待完成，不重复点击。被停稳/时序检查拦截时保存原因，不绕过。
4. 30秒静止检查通过后，加载预览，核对 XY 地标与 XZ 高度/楼层，再人工确认。不要用旧 `/localization` 室内地图页面判定 v3。
5. 先录10秒静止，确认真实文件及质量结果正常，再手柄控制安全平地直行和转向短段。网页结束录制不会停车。
6. 首段起点、拐弯和安全终点停稳后标3–5个位置草稿。当前参考点未核验，不能直接交给导航执行；楼梯不做自主测试。
7. 手柄停稳，汇总并下载报告；回到有网处取回原始 bag，做地图绑定、时序/外参审阅与离线影子策略检查，再决定下一次外出。

本轮现场目标仅是：v3定位证据、真实短录制、首段位置草稿；不是全程自主验收。掉定位、点云持续过期或控制权不明确时停止推进并留证。
