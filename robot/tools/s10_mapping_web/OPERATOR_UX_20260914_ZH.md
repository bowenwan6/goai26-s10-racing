# 现场助手：操作指引与等待状态更新

2026-09-14 18:55（中国时间）完成最后一轮部署与只读检查。

## 这次的修改计划与完成情况

1. **先明确当前步骤**：顶部显示本次目标、实际地图、当前卡在哪一步和下一步。自检通过不等于目标图已经加载。
2. **把失败写成人能执行的提示**：按钮旁解释为什么不能点。命令、运动反馈分列，停稳检查失败保留诊断证据；不放宽门限、不绕过停稳保护。
3. **处理期间明确等待**：请求提交、排队、采样、录制、核验、保存分别说明；执行期间任务按钮禁用，当前按钮带沙漏与阶段文字。只有后台给出采样秒数时显示确定进度，没有可靠剩余时间时显示持续等待条。30秒/10秒/3秒不是整个任务保证耗时，检查和文件收尾完成后才恢复按钮。
4. **区分保存和通过**：草稿保存成功、录制质量未通过、报告已生成但验收未通过分别显示；不把 SUCCEEDED 直接当作验收绿灯。
5. **WP 与报告覆盖完整会话**：展示已保存清单与数量，失败点击不计数、同名不覆盖；汇总后继续补点会提示重新汇总。新报告不再受最近100条任务列表限制，历史草稿保留原地图/设备绑定，不能自动拼成导航路线。
6. **减轻状态查询延迟**：新增聚合 dashboard，一次后台往返取得健康、状态与会话概况；避免多个串行 SSH 查询令实时快照过期。原有新鲜度标准没有放宽。

以上均已完成。入口仍是机器人 Wi-Fi 内 `http://10.21.41.1:8080/` 首屏绿色“现场助手”，直接地址 `/field`。刷新页面后使用；不需要互联网、Codex 或在线资源。

## 给现场操作者

- 新建/恢复 v3 会话，先自检，看顶部“实际加载地图”，不要仅凭灰色地图预览判断切图成功。
- 退出所有人的策略程序，手柄停稳并确认接管后加载目标图。失败时读②诊断；不要连点或绕过。
- 地图加载成功后做30秒静止检查，保持不动直到最终结果；看XY地标与XZ楼层，保存人工确认。
- 先试10秒静止录制和一个WP。3秒标点同样必须等“草稿保存成功”且清单加一；之后可继续补齐30个WP。
- 录制结束、汇总、按钮变灰都不是停车或急停，运动始终由原厂手柄负责。
- 任何掉定位或重启都需要重新检查；手机断线后恢复同一会话查询，不能猜测是否完成后反复点。

完整六步及参数见 [FIELD_GUIDE_ZH.md](FIELD_GUIDE_ZH.md)。

## 验证结果

- 独立 Python QA：83项通过；原 field 单元测试：7项通过。
- Node 指引/门禁/进度测试：22项通过；原建图服务器与定位视图回归检查通过。
- 390×844 手机尺寸的隔离演示：30秒检查、10秒录制、3秒标点期间禁用按钮；区分模拟录制质量失败；完成后恢复按钮；WP01→汇总→WP02→提示报告过期→新汇总计数2→刷新恢复均验证。页面宽度和内容宽度均390，无横向溢出，浏览器无错误日志。
- 真机认证后 `/`、`/field`、`/field.js`、`/localization`、`/heightmap` 均 HTTP 200；生产 `demo:false`、`ui_contract:2`，dashboard 约2.32秒往返，无活动任务。前端/后端生产文件 SHA256 与本地一致。
- 只在本地 fake adapter 运行流程按钮，真机没有提交切图、采样、录制、标点或运动操作。没有修改标定、网络、原厂定位或规划器配置。

只读 HTTP 证据：工作区 `map-reviews/0914_fr_v3-20260914-142008/app-guidance-20260914/http-progress-final-check.json`。

## 真机当前边界与异常记录

- 实际地图仍是 `indoor_loop_01-20260911-203447`，目标 `0914_fr_v3-20260914-142008` 尚未加载。
- 早先反馈非零导致加载在调用官方激活前被阻断；没有保存此前尝试的wp1–wp3。原会话仍12条操作，已保存位置0，旧汇总未通过。
- 本轮18:50附近连接断开后，106 boot ID 从 `31447775-cd84-425b-8989-a197c0484534` 变为 `0d97d1bc-bae8-47a4-aa6c-70f86cda9220`，说明发生了机器重启。本次没有发送重启机器的命令，不能推断重启原因。
- 重连后、最终部署前后：定位 Invocation 均 `c879fe45608f412b9852a5d935a0308b`，规划器 Invocation 均 `a27b7855c505457e8163150077fe0fb3`；命令与反馈在只读快照中均 `[0,0,0]`、无目标。此前非零反馈的根因未确定，不能以一次恢复宣称已修好。
- 只重启102网页服务及106现场助手；未切图、未启动定位或规划器、未录制、未控制行走。旧定位检查在重启后失效是预期保护。
- 未完成：实际v3加载、现场静止/动态定位验证、真实短录制、真实手机无WAN冷启动验收。当前位置参考点仍未核验，WP只能保存草稿；不授权自主行走。

原始失败报告未改写，SHA256 保持 `c7ad0374596fd8a5604abf854351c0d9d70d6fe9f652587beae9a2f54246f30e`。

## 备份与最终部署清单

本轮修改前备份（原件未被后续迭代覆盖）：

- 102：`/home/golai/s10-guidance-20260914.wWUZJg/before/`，原 server/core/JS/HTML。
- 106：`/home/user/s10-guidance-20260914.gDcO7n/before/`，原 core/worker/robot。

最终生产 SHA256：

| 文件 | SHA256 |
| --- | --- |
| field_core.py | bd44ec44104d2a9cc55d72d081003b0a3d6782192a5a96ad9f84b1b9c360f2e1 |
| field_worker.py | 0516e37fa1a5769653b013b5f33879ef4ee5b9138a3e6933e4de0ce94ea78bab |
| field_robot.py | 8af30059a8460d1974e9dc6621a4b03eb809549417253caed0c3fccb0a1b8a92 |
| field.js | 201d6d48dfb6d4b0f30042e5ab9043a282335670a4e0f0b11d349846b2727f5f |
| field.html | ae1f62b4a0f12444d7861f72714e36d77e6ce9ce06c1e3821279ae775a07462a |
| server.py | e6df0e16148041d4d09d34d201245782c0a4707280802090b07df9ff2fb0368c |

最终服务：106 `s10-field-worker.service` active、PID17610；102用户 `s10-mapping-web.service` active、PID5449；各 NRestarts=0。PID仅记录当时状态。

回滚须先手柄停稳、确认无活动任务并取得应用 operation.lock；停现场助手，恢复上述明确备份中的代码并重启两项应用服务。数据根和原报告不删除，回滚不切换地图。不要覆盖认证密钥、其它网页或用户新改动。

本轮使用 API Testing 与 Playwright E2E 检查接口和手机交互；Safety System 的失效关闭原则使未知状态、非零反馈和无效旧检查继续阻断。没有降低检查标准，也没有宣称功能安全认证。未提交或推送Git。
