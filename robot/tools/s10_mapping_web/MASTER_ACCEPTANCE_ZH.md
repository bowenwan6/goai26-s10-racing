# 首版 master 验收记录

日期：2026-09-13。开发分支：`codex/field-assistant`；实际部署源码基线：`889e4d8`。

## 结论与交付范围

首版完成本地实现和独立检视返工；**不是已部署、真机验收通过或允许自主行走的结论**。

按用户要求由两位成员协作：implementer 负责应用实现，reviewer 独立编写测试、运行、提出问题并复测；master 核对方案边界、只读实机事实并总复测。没有第三个成员，没有修改备份快照。

新增 `/field` 六步：自检、停稳后官方切图、静止定位与人工叠合、限时原始录制、位置草稿/待审航点、报告下载。沿用原登录及受限SSH；保留原建图、定位和高度图。未增加运动发布、导航目标、实机bag播放或公网依赖。

## master 亲自复核的证据

- Python 3.12 独立 QA 最终 **48/48** 通过，实际运行约14.5秒，包含录制监护的真实10秒本地子进程计时；不启动真实ROS录制。
- 实现自测 **5/5**；旧建图服务、定位页面 **2项回归**通过；新JavaScript及启动Shell语法检查通过。
- 真实 fullrun PCD 在电脑本地抽样得到29,897点，紧凑JSON约684KB；传输已覆盖这一实际规模。仅用于显示，不是碰撞几何。
- 只读106环境确认ROS Jazzy/rosbag2 0.26.9、MCAP插件、独立持久分区；`/ODOM` 子帧为空、协方差全零，不能猜测其导航参考点或精度。
- 在电脑本地用只读取得的106实时规划器完整日志及106时钟运行新解析器，能够识别当时的空闲/零命令状态；这不是安全停车认证。
- 发现并交回修复ROS环境脚本不能在 `nounset` 下加载的问题；仅在106运行既有ROS环境加载及库导入的只读等效命令，修正后退出码0。
- 核对成功390px手机视口截图；浏览器全流程由独立reviewer执行，详细结果与环境限制见 [QA报告](qa/REVIEW.md)。

主机根目录虽然是overlay，其upper在ext4，不应声称一定断电丢失。应用仍使用专用数据分区。Mac与106观察到约3.5秒壁钟差；ROS/日志的源时间必须与106同一时域比较，手机仅使用接收后的相对时长判断显示是否过期。

## 尚未完成、不能跳过的现场环节

1. 值守停稳时按 [使用与部署回滚指南](FIELD_GUIDE_ZH.md) 部署，核对实际106设备身份，保持参考点标定为未验证。
2. 先只读自检和地图预览，再安排官方切图、30秒定位检查及10秒录制实机验收，监测106负载和传感器频率。
3. 手机关闭蜂窝数据、机器人无WAN，验首次打开、锁屏重连、下载与冷启动内部授时。
4. 回到地图现场核对地标、方向和楼层；当前参考点未核验时只保存位置草稿。之后在电脑离线影子回放并人工审路线，再讨论导航接入和分段实车测试。

本轮没有部署、重启机器人服务、切换实机地图、录制实机bag或发出运动指令。安全、TF2和离线存储技能用于失败关闭、坐标身份校验和持久任务设计，不构成功能安全认证。
