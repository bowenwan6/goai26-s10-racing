# 2026-09-13 实机上线记录

## 当前可用入口

手机连接机器人热点，打开 `http://10.21.41.1:8080/`，点击首屏绿色 **现场助手**。直接入口 `/field` 也已安装。沿用原网页登录账号和密码；服务升级使旧登录会话失效时重新登录即可。

本次修复用户遇到的 `{"detail":"没有这个页面"}`：此前只是本地开发完成，102实际仍是旧server，106未安装field worker，并非手机地址输错。

## 已执行及实际验证

- 106安装独立系统服务 `s10-field-worker.service`，已启动并设为开机启用；真实设备身份与106 machine-id哈希匹配，未启用fake/demo。
- 102更新server、首页、field页面/脚本与共用校验模块，保留认证配置、SSH密钥、受限命令，以及原定位/高度图文件。103热点转发不改，Tailscale/Clash配置不改。
- 从运行Codex的电脑直接经机器人热点地址访问，不是loopback模拟：原凭据登录200；`/`、`/field`、`/field.js`、`/localization`、`/heightmap`均200且内容标记匹配。
- `/phone/field/health`返回`worker:true, demo:false`；live返回真实身份，传感器数据来自106。
- 实际通过HTTP提交**仅自检**任务，`SUCCEEDED`且数据自检`passed:true`，参考点/外参明确`unknown`，互联网`not_required`。没有把任务成功当作全局定位通过。
- 自检job：`cad692042d584ba09ed690d197f94ff9`；会话：`354d9cbcb2d14988899a9c1e60629c01`。这是一条部署验收记录，可以在手机恢复会话中查看；保留以便审计，不是现场走测。
- 本次目标 `1209_01_F-20260912-175844` 地图预览HTTP200，29,897个显示抽样点，约774KB；原始地图未改写。
- 后端存储目录0700、Unix socket0600，属于user；配置root:user、0640。无任意文件下载或命令入口。

本地首页专项验证了390px大按钮、未登录点击后保持`/field`完成登录、已有登录直接进入；源码与实际部署文件核对。Codex内嵌浏览器验证工具对该局域网导航超时，**没有把它记为实机浏览器通过**；实际热点HTTP验收与本地浏览器渲染测试分开记录，真实手机仍应点开确认。

## 真机暴露后已修的问题

1. 106的systemd RuntimeDirectory创建报233，尽管`/run`本身存在。未改厂商全局运行目录，改用私有持久应用根中的`worker.sock`；保留单实例、启动残留清理及普通文件/symlink拒绝。
2. 慢厂商状态查询期间ROS接收线程继续更新，旧实现用查询前时刻减新接收时刻，误算负age。改在持有数据锁时统一采样时间，实际流age恢复正值，没有强制归零或放宽时序门限。
3. 显示点云原有0.5秒抽样间隔，加上传感器源延迟会使自检偶发误拒；将显示解码间隔减为0.2秒。原始录制和0.5秒源时间门限不变，不以放宽门槛制造通过。

独立补充测试：socket兼容9项，snapshot时序6项；原独立回归48项仍通过。首页、本地浏览器与各专项证据见`qa/`。

最终master总测：QA合计63/63、实现自测6/6、snapshot自测2/2、首页认证回归1/1、旧回归2/2全部通过。最后10次实机读样本中点云源age约0.158–0.408秒，对齐点云约0.053–0.305秒，没有通过放宽门槛取得结果；这只是短时观测，不代表长时压力测试。worker保持active/enabled、NRestarts=0，内存约159MB。102的5个及106的7个部署源码SHA256与本地已测文件逐字节一致，见`DEPLOYED_HASHES_20260913.json`。

## 备份与恢复边界

- 102现版覆盖前备份：`/home/golai/s10-field-before-foxtxp8n`（server/index）。
- 106现版覆盖前备份：`/home/user/s10-field-before-rew1k3r3`（robot_backend/backend/heightmap）。
- 106持久任务及产物：`/var/opt/robot/data/s10_field_assistant`；回滚保留，不删除。
- 配置和认证原件未覆写。原GitHub备份快照未改。

回滚需先停稳、确认无活动建图/录制/field任务，再停新worker、恢复上述指定源码并重启102网页。不要删除任务数据，不自动恢复或切换地图。

## 尚未完成的现场验收

上线后active仍为旧图 `indoor_loop_01-20260911-203447`，定位/规划器InvocationID未变；当时厂商上报“定位丢失／局部”，**不能据此导航，也不能据此评价尚未加载的新图**。本轮没有切图、录实机bag、发运动或重启定位/规划器服务。

下一步由现场人员用手柄停稳，在新图覆盖区域加载本次图，再执行30秒静止检查与人眼叠合、限时真录制及草稿标点。参考子帧仍为空，不能生成已核验导航航点。手机关闭蜂窝数据/机器人无WAN、系统冷启动、真实MCAP负载和现场几何定位尚未验收；不会因为网页返回200而声称这些已通过。
