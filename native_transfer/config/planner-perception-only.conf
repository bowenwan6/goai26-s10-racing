# GOAI native follower owns navigation. Preserve the original NAV_POINTS producer.
# Restore vendor planning by removing ONLY this drop-in and restarting planner.
[Service]
ExecStart=
ExecStart=/usr/bin/chrt 50 /usr/bin/taskset -c 4,5 /opt/robot/share/planner/bin/pcl_remove
